security:
    # Hiérarchie des rôles - À METTRE ICI, pas dans when@test
    role_hierarchy:
        ROLE_ADMIN: ROLE_EMPLOYE
        ROLE_EMPLOYE: ROLE_USER

    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        api_docs:
            pattern: ^/api/(docs|contexts|\.well-known)
            security: false
        login:
            pattern: ^/api/login$
            stateless: true
            security: false 
            lazy: true
            provider: app_user_provider
            
        main:
            pattern: ^/api
            stateless: true
            lazy: true
            provider: app_user_provider
            custom_authenticator: App\Security\ApiTokenAuthenticator
            # Choisir selon le besoin :
            # stateless: true   # Pour API pure (JWT, tokens)
            # stateless: false  # Pour garder les sessions


    # Note: Only the *first* matching rule is applied
    access_control:
        - { path: ^/api/docs, roles: PUBLIC_ACCESS }
        - { path: ^/api/contexts, roles: PUBLIC_ACCESS }
        - { path: ^/api$, roles: PUBLIC_ACCESS }
        - { path: ^/api/login, roles: PUBLIC_ACCESS }
        - { path: ^/api/registration, roles: PUBLIC_ACCESS }
        # Routes admin 
        - { path: ^/api/employes/\d+/toggle, roles: ROLE_ADMIN } 
        - { path: ^/api/admin, roles: ROLE_ADMIN }  
        # Les utilisateurs connectés peuvent modifier/supprimer leur compte
        - { path: ^/api/users/\d+, roles: ROLE_USER, methods: [GET, PUT, DELETE] }
        # Admin peut tout faire
        - { path: ^/admin, roles: ROLE_ADMIN }
        - { path: ^/employe, roles: ROLE_EMPLOYE }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10